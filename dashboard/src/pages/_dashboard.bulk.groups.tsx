import GroupOperations from '@/components/bulk/GroupOperations'

/**
 * Bulk Groups Operations Page
 * 
 * Renders the GroupOperations component for bulk group management.
 * This page is accessed via the /bulk route and displays group-related
 * bulk operations interface.
 */
export default function BulkGroupsPage() {
  return <GroupOperations />
} 