import UserOperations from '@/components/bulk/UserOperations'

/**
 * Bulk Users Operations Page
 * 
 * Renders the UserOperations component for bulk user management.
 * This page is accessed via the /bulk/users route and displays user-related
 * bulk operations interface.
 */
export default function BulkUsersPage() {
  return <UserOperations />
} 